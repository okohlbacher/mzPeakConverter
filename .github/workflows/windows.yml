name: Windows

# Validates the converter on the real Windows target:
#  - `windows` job: builds default + all Windows-only vendor-SDK features
#    (Agilent MHDAC / SciEX Clearcore2 / Bruker BAF FFI), the C# interop glues,
#    runs the tests, and smoke-converts a tiny bundled mzML.
#  - `msconvert` job: installs ProteoWizard and exercises the --via-msconvert lane.
# Runtime conversion of actual Agilent/SciEX/BAF *data* via the native readers
# still needs the proprietary vendor DLLs + vendor data and stays out of CI.

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

concurrency:
  group: windows-${{ github.ref }}
  cancel-in-progress: true

jobs:
  windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      # SQLite is compiled from source (rusqlite `bundled`), and .NET hosting for
      # the Thermo reader downloads nethost at build — no system deps to install.
      - name: Build (default features)
        run: cargo build --release --verbose

      - name: Build (all vendor features — Agilent + SciEX + Bruker BAF FFI)
        run: cargo build --release --features "agilent sciex bruker_sdk" --verbose

      - name: Test
        run: cargo test --release --verbose

      - name: Build C# interop glues (reflection-only, no vendor DLLs)
        run: |
          dotnet build glue/sciex/SciexGlue.csproj -c Release
          dotnet build glue/agilent/AgilentGlue.csproj -c Release

      # Core convert path, end-to-end on Windows, against a tiny committed mzML.
      - name: Smoke test — convert + round-trip verify
        run: cargo run --release -- convert tests/fixtures/tiny.pwiz.1.1.mzML -o out.mzpeak --verify --force

      # SciEX .wiff with the feature OFF (default `cargo run`) must report the
      # unsupported-vendor contract — exit 3, a clean refusal, not a crash.
      - name: Vendor-guard contract (SciEX .wiff without feature → exit 3)
        shell: pwsh
        run: |
          New-Item -ItemType File -Force -Path sample.wiff > $null
          cargo run --release -- convert sample.wiff -o out.mzpeak --force
          $code = $LASTEXITCODE   # capture before it leaks into the step's own exit status
          if ($code -ne 3) { Write-Error "expected exit 3, got $code"; exit 1 }
          Write-Host "vendor guard returned exit 3 as expected"
          exit 0

  # Installs ProteoWizard (msconvert) and exercises the cross-vendor --via-msconvert
  # lane end-to-end on Windows. Uses the committed mzML (msconvert reads mzML and
  # re-emits it → we re-ingest), so the full lane — exe resolution, subprocess,
  # temp mzML handling, re-ingest — is tested without needing licensed vendor data.
  # Separate job so a flaky pwiz install can't mask the core build validation.
  msconvert:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Install ProteoWizard (msconvert) via Chocolatey
        shell: pwsh
        run: choco install proteowizard -y --no-progress --ignore-checksums

      - name: Locate msconvert.exe
        shell: pwsh
        run: |
          $exe = Get-ChildItem -Path 'C:\ProgramData\chocolatey','C:\Program Files','C:\Program Files (x86)' `
                   -Recurse -Filter msconvert.exe -ErrorAction SilentlyContinue |
                 Select-Object -First 1
          if (-not $exe) { Write-Error "msconvert.exe not found after install"; exit 1 }
          Write-Host "msconvert: $($exe.FullName)"
          "MSCONVERT_PATH=$($exe.FullName)" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8

      - name: Build
        run: cargo build --release --verbose

      - name: Convert via msconvert (mzML -> msconvert -> mzPeak)
        shell: pwsh
        run: |
          cargo run --release -- convert tests/fixtures/tiny.pwiz.1.1.mzML --via-msconvert -o out_pwiz.mzpeak --force
          $code = $LASTEXITCODE
          if ($code -ne 0) { Write-Error "--via-msconvert failed: $code"; exit 1 }
          if (-not (Test-Path out_pwiz.mzpeak)) { Write-Error "no output archive produced"; exit 1 }
          Write-Host "--via-msconvert produced $((Get-Item out_pwiz.mzpeak).Length) bytes"
          exit 0
